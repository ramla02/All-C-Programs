#include <stdio.h>

/**
 * main - the main function and the start of the program
 * A c program that prints numbers from 1 to 100
 * Return: 0 for the successful execution
 */

int main(void)
{
	int i;

	for (i = 1; i <= 100; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
